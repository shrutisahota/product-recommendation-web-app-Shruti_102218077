# Shruti_102218077 Frontend

## Run
```
cd frontend
npm install
cp .env.example .env
# set VITE_API_BASE to your backend (ngrok URL from Colab)
npm run dev
```

Routes:
- `/` – Recommend page (query → results + GenAI desc)
- `/analytics` – Dataset charts
